$(document).ready(function () {
	$('#tabs').tabs();
	//textarea = $('#xml .xmlcode').val();
	//lines = textarea.split("\n");
	//console.log(lines.length * 20 + 'px');
	//$('#xml .xmlcode').attr('height', lines.length * 10 + 'px');
});
